name: woodwright
description: Woodwright — woodworking planner with cut lists, calculators, and joinery guidance.
publish_to: "none"
version: 0.1.1+2

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.2
  shared_preferences: ^2.2.3
  path_provider: ^2.1.4
  uuid: ^4.4.2
  share_plus: ^10.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
